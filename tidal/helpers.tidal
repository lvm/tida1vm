-- just a couple of custom params

let (att, _) = pF "att" (Just 0)
    (dec, _) = pF "dec" (Just 0.25)
    (sus, _) = pF "rel" (Just 1)
    (rel, _) = pF "rel" (Just 0.75)
    (freq, _) = pF "freq" (Just 440)
